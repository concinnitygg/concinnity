[package]
name = "concinnity-slang"
version.workspace = true
authors.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
repository.workspace = true
homepage.workspace = true
description = "The slangc invocation shared by Concinnity build scripts and the renderer"
readme = "README.md"
keywords = ["gamedev", "shader", "slang", "concinnity"]
categories = ["game-engines", "rendering::graphics-api"]
include = [
    "/README.md",
    "/src",
    "/build.rs",
]

[features]
# Turns a missing slangc from a silent test skip into a panic. For a host that
# carries the toolchain: the gauntlet's all-features tier enables it.
strict-shader-tests = []

[lints]
workspace = true

[dev-dependencies]
concinnity-testing = { workspace = true }
