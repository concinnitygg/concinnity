[package]
name = "concinnity-templates"
version.workspace = true
authors.workspace = true
edition.workspace = true
license.workspace = true

[lib]
name = "concinnity_templates"
doctest = false

[dependencies]

[dev-dependencies]
# Validates that every template's embedded JSONL parses; test-only.
serde_json = { version = "1", features = ["preserve_order"] }
